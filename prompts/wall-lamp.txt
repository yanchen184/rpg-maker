Use your image_gen tool to generate exactly ONE PNG image, 1024x1024, with a fully transparent background (alpha channel). Do nothing else (no file operations, no code).

Spec:
- It is a sprite sheet: a 2x2 grid of 4 animation frames. Frame order: top-left, top-right, bottom-left, bottom-right. Each frame occupies exactly one 512x512 quadrant.
- Subject: a small warm wall-mounted lamp (front view) with a glowing round bulb and a soft warm halo, for a top-down 2D RPG interior wall (like Stardew Valley). 16-bit pixel art.
- Animation across the 4 frames: the lamp fixture stays pixel-identical; only the warm glow/halo pulses gently (slightly brighter, slightly dimmer), smooth loop.
- Background: fully transparent. No wall behind.
- No grid lines, no labels, no text.
