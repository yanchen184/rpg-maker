Use your image_gen tool to generate exactly ONE PNG image, 1024x1024. Do nothing else (no file operations, no code).

Spec:
- It is a sprite sheet: a 2x2 grid of 4 animation frames. Frame order: top-left, top-right, bottom-left, bottom-right. Each frame occupies exactly one 512x512 quadrant, filled edge-to-edge.
- Subject: a seamless tileable lush green grass meadow texture, viewed straight top-down, 16-bit Stardew Valley outdoor pixel art style, warm sunny palette, small tufts and a few tiny wildflowers. The texture must tile seamlessly (left edge matches right edge, top matches bottom).
- Animation across the 4 frames: grass layout stays pixel-identical; only tiny grass tufts sway subtly as in a light breeze, forming a smooth loop.
- Opaque image, no transparency, no objects, no grid lines, no labels, no text.
