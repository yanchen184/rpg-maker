Use your image_gen tool to generate exactly ONE PNG image, 1024x1024, with a fully transparent background (alpha channel). Do nothing else (no file operations, no code).

The ATTACHED IMAGE is a character idle sprite sheet: a 4x4 grid of 16 frames (each cell 256x256). Row 1 = standing facing the viewer (down), Row 2 = facing left, Row 3 = facing right, Row 4 = facing away (up, back of head). The 4 frames per row are a subtle breathing loop. The character has short dark-brown hair.

Your task: generate a PAPER-DOLL HAIR OVERLAY sheet. Think of it as RECOLORING AND RESTYLING ONLY THE EXISTING HAIR of the attached character:
- Same 4x4 grid, same 256x256 cells.
- Each cell contains ONLY hair pixels (no head, no face, no eyes, no body): a golden-blonde SHORT tidy hairstyle.
- CRITICAL SIZE RULE: the blonde hair must have exactly the same silhouette, size and position as the dark-brown hair already on the character in the SAME cell of the attached sheet. It is a hair "cap" covering only the scalp - top and back of the head. It must NOT hang below the ears, must NOT cover the face or eyes (rows 1-3 keep the face fully visible), and must NOT touch the shoulders or body.
- Match the head position frame by frame, following the subtle breathing motion.
- 16-bit pixel art matching the attached sheet's style.
- Everything except the hair is fully transparent. NO checkerboard, NO background, no grid lines, no labels, no text.
